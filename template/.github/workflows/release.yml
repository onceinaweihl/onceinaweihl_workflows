name: Release Please

on:
  push:
    branches: [main]

permissions:
  contents: write
  pull-requests: write

jobs:
  release-please:
    uses: onceinaweihl/onceinaweihl_workflows/.github/workflows/reusable-release-please.yml@main
    secrets: inherit
